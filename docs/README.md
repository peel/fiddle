# Project Documentation

Living docs for product and technical decisions. Persistent knowledge that informs all work — separate from beans (actionable work) and plans (session-scoped execution).

**Flow:** `/discover-docs` → `/challenge` → `superpowers:brainstorming` → `/panel` → `/challenge` → `superpowers:writing-plans` → beans → `/develop` → `/deliver-docs`

**Feedback loop:** `/feedback` (capture) → `/insights` (synthesize) → discover/brainstorm (consume) → features → `/feedback`

## Contracts

- [`fiddle-agentic-factory-prd.md`](fiddle-agentic-factory-prd.md) — the approved product and interface contract for the Agentic Factory V1 milestone series (M0 onward). It defines the V1 product scope, the typed capability and orchestration interfaces, the non-goals and boundaries, and the milestone acceptance criteria. Beans and plans in that series cite this file by path and commit (`docs/fiddle-agentic-factory-prd.md@<sha>`) rather than any machine-local copy; when implementation and this document disagree, this document is the contract and must be amended deliberately.

## Structure

```
docs/
├── product/
│   ├── VISION.md        — what, who, why, non-goals
│   ├── MARKET.md         — landscape, competitors, positioning
│   ├── PRICING.md        — business model, costs, revenue
│   ├── GTM.md            — distribution, channels, messaging
│   ├── FEEDBACK.md       — structured user signals (append-only)
│   ├── personas/         — reusable persona profiles (generated by /insights)
│   │   └── <slug>.md     — one file per identified persona
│   ├── insights/         — feedback synthesis summaries (generated by /insights)
│   │   └── YYYY-MM-DD-synthesis.md
│   ├── templates/        — product artifact templates (project-supplied)
│   │   └── <artifact>.md — instructions for generating release notes, social, etc.
│   └── releases/         — generated product artifacts (from deliver phase)
│       └── YYYY-MM-DD-<epic>-<type>.md
├── technical/
│   ├── SYSTEM.md         — how it works now
│   ├── decisions/
│   │   └── NNN-title.md  — ADRs (append-only)
│   └── RUNBOOKS.md       — deploy, rollback, common issues
├── fiddle-agentic-factory-prd.md — approved product/interface contract, Agentic Factory V1
└── BACKLOG.md            — pre-bean ideas and debt (append-only)
```

## Skills

- `/discover-docs [scope]` — Socratic dialogue to bootstrap or review docs
- `/deliver-docs [--epic <id>]` — post-ship update of technical docs, ADRs, backlog
- `/adr <title>` — new architecture decision record
- `/feedback <signal>` — append structured user feedback (who, context, observation, implication, confidence)
- `/insights` — synthesize feedback into persona files and insight summaries
- `/backlog <idea>` — append idea or debt item

## Conventions

- Product docs: overwrite freely. Technical decisions: append-only, supersede with new records.
- Append-only logs (FEEDBACK, BACKLOG, decisions/) are never edited or deleted.
- Every curated doc has a `Last reviewed:` date.
- Keep it short. A doc you won't read doesn't exist.
